# W11 long-verify · BLOCKER · 2026-07-24

> base tip: `df11a2f2` · wt: `cp-long-verify` (removed after record)

## Commands + exit

| 命令 | exit | 备注 |
|------|------|------|
| `tsc --noEmit` | **2** | 仅 `tests/probe-security-headers.test.ts` ProcessEnv（W5 feature `6015f650` 已修，未合 master） |
| `vitest run` | **0** | 62 files / **614** pass · 6 skip |
| `pnpm run build` (`next build --webpack`) | **1** | 见下 |

## Build fail root cause

```
.next/types/app/api/csp-report/route.ts
Property 'toPathOnlyUri' is incompatible with index signature.
Type '(value: unknown) => string' is not assignable to type 'never'.
```

- `app/api/csp-report/route.ts` **export function toPathOnlyUri**（约 L22）
- Next 16 App Router 路由模块禁止导出非 handler 符号
- 引入 commit：`24439d1b` fix(security): path-only sanitize csp-report URIs
- **已在 origin/master**（非本波回归）
- 测试从 route 再 export 导入：`tests/api-csp-report.test.ts`

## Fix plan (next wt)

1. 将 `toPathOnlyUri` 移到 `lib/csp-report-uri.ts`（或同类）
2. route 仅 `import` 不 re-export
3. 测试改 import 源
4. 重跑 `pnpm build` exit 0 + csp-report vitest

## Status

```text
module: M-CP-long-verify
status: BLOCKED
blocker: CP-CR-BUILD-route-export
push_master: NOT_DONE
```
